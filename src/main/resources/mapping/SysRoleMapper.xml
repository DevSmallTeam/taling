<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uxunchina.taling.system.mapper.SysRoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.uxunchina.taling.system.entity.SysRole">
        <id column="role_id" property="roleId" />
        <result column="available" property="available" />
        <result column="creat_time" property="creatTime" />
        <result column="description" property="description" />
        <result column="role" property="role" />
    </resultMap>

    <!--根据角色名称获取角色信息-->
    <select id="getRoleByName" resultMap="BaseResultMap">
        SELECT * FROM SYS_ROLE WHERE ROLE = #{roleName}
    </select>

</mapper>
